providers = ["python"]

[phases.install]
cmds = [
  "pip3 install -r price_tracker/requirements.txt",
  "playwright install --with-deps chromium"
]

[phases.build]
cmds = ["cd price_tracker && python3 manage.py collectstatic --no-input"]
